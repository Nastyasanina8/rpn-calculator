{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build", // ← ЭТО ИМЯ ССЫЛАЕТСЯ ИЗ LAUNCH.JSON
            "type": "shell",
            "command": "cmake",
            "args": [
                "--build",
                "build"
            ],
            "group": "build"
        }
    ]
}